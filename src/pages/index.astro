---
// Esta página no debería renderizarse nunca: la redirección "/" -> "/es/"
// está configurada en astro.config.mjs (redirects) y en public/_redirects
// (para Cloudflare Pages en producción). Se deja como fallback de seguridad.
return Astro.redirect('/es/');
---
